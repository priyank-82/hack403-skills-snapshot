version: '3.8'

services:
  skillmorebackend:
    build: .
    ports:
      - "8000:8000"
      - "5678:5678"
    environment:
      - DEBUGPY=1
    volumes:
      - .:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"] 